import React from 'react';
import { Styled } from 'direflow-component';

const Grid = () => {
  return (
    <Styled styles={styles}>
      <div className='app'>
        Grid
      </div>
    </Styled>
  );
};

export default Grid;
