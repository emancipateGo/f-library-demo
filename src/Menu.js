import React from 'react';
import { Styled } from 'direflow-component';

const Menu = () => {
  return (
    <Styled styles={styles}>
      <div className='app'>
        Menu
      </div>
    </Styled>
  );
};

export default Menu;
