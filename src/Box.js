import React from 'react';
import { Styled } from 'direflow-component';

const Box = () => {
  return (
    <Styled styles={styles}>
      <div className='app'>
        Box
      </div>
    </Styled>
  );
};

export default Box;
