import React from 'react';
import { Styled } from 'direflow-component';

const Card = () => {
  return (
    <Styled styles={styles}>
      <div className='app'>
        Card
      </div>
    </Styled>
  );
};

export default Card;
